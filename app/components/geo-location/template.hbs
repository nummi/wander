{{#if geolocation.getPosition.isRunning}}
  {{yield to="inverse"}}
{{else}}
  {{yield (hash accuracy=coords.accuracy
                altitude=coords.altitude
                heading=coords.heading
                latitude=coords.latitude
                longitude=coords.longitude
                speed=coords.speed)}}
{{/if}}
